import React, { useState } from "react";


export default function ReplyButton() {

   return (
      <button
         className="post-UI-button"
      > 
        <img className="post-UI-image-reply" src="../SceneStream/content/Images-UI/reply.png" />
      </button>
   );
}

